<view class="record-page">
    <view class='record-wrapper'>
        <view class='record-item' wx:for="{{arr}}" wx:key="index">{{item}}</view>
    </view>
    <button bindtap='clearRecord'>清除历史记录</button>
</view>